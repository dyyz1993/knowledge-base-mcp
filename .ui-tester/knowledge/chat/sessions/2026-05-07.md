# Session: ui-test-1778090993
Date: 2026-05-07
Module: chat
Scenarios: 6 passed / 3 failed / 2 partial

## Tested
- Page loads correctly: PASSED
- Sidebar shows session list: PASSED
- Can create new session: PASSED
- Send message and get AI response: PASSED
- AI response renders with Markdown (bold, lists, code blocks, emojis): PASSED
- Parallel sessions work: PASSED (both sessions respond independently)
- Session name auto-update: FAILED (stays as "Chat xxxxx" ID)
- Running status indicator: PARTIAL (could not observe spin icon, AI responds too fast)
- Scroll behavior: PARTIAL (auto-scroll to bottom on new message works, user scroll-up is preserved, but initial send auto-scrolls to bottom)
- Copy buttons: PASSED (message copy + code block copy both present with hover reveal)
- Model selector position: FAILED (in header bar at top, not near input as spec requires)
- Suggested topics: FAILED (no suggestion buttons appear after AI response)
- Mobile responsive: FAILED (no hamburger menu, sidebar uses fixed widths)
- Knowledge base search: PASSED (AI calls kb_outline, kb_search, kb_read tools)

## Findings
- textarea fill doesn't trigger React onChange; must use type + Enter key dispatch
- Chat area selector: document.querySelectorAll('.flex-1.overflow-y-auto')[1]
- Sidebar session list: document.querySelectorAll('.flex-1.overflow-y-auto')[0]
- AI responses include full Markdown rendering with syntax-highlighted code blocks
- Model selector value: "GLM-4.7 (zhipuai)"
- Page uses Ant Design select component for model chooser
