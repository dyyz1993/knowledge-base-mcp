# Session: ui-test-ask-1779682602
Date: 2026-05-25
Module: ask
Scenarios: 20 tested / 20 passed / 0 failed

## Tested
- TC-ASK-01: Empty state Sparkles icon + "智能问答" title — PASSED
- TC-ASK-02: Description text about KB-first search — PASSED
- TC-ASK-03: "普通查询" feature card with amber label — PASSED
- TC-ASK-04: "Agent 深度研究" feature card with purple label — PASSED
- TC-ASK-05: Suggested queries visible — PASSED (2 suggestions found)
- TC-ASK-06: Click suggestion fills textarea — PASSED (find text click works, DOM eval click doesn't)
- TC-ASK-07: Input placeholder text — PASSED ("你想了解什么？描述一下你的问题...")
- TC-ASK-08: Textarea auto-resize — PASSED (36px → 96px with multi-line)
- TC-ASK-09: 3 mode buttons exist — PASSED (Quick/Standard/Deep)
- TC-ASK-10: Standard default selected — PASSED (aria-pressed=true, purple style)
- TC-ASK-11: Quick mode switch — PASSED (aria-pressed toggles, amber style)
- TC-ASK-12: Deep mode switch — PASSED (aria-pressed toggles, blue style)
- TC-ASK-13: Send button disabled when empty — PASSED (disabled=true, opacity-30)
- TC-ASK-14: Agent Research button disabled when empty — PASSED (disabled=true, opacity-30)
- TC-ASK-15: Buttons enabled after input — PASSED (both disabled=false after typing)
- TC-ASK-16: Model selector exists — PASSED (HTML select element)
- TC-ASK-17: Default model selection — PASSED ("默认模型" selected, GLM-4.5 Air)
- TC-ASK-18: Input query and send — PASSED (POST /api/kb-ask triggered)
- TC-ASK-19: Loading state during query — PASSED ("停止查询" button, pulse animation)
- TC-ASK-20: Query result displayed — PASSED (多源搜索 result card with sources)

## Findings
- Mode buttons use SVG icons only (no visible text), identified by aria-pressed and index
- Suggestion click requires agent-browser find text click — DOM eval click doesn't trigger React state
- Textarea requires nativeInputValueSetter for programmatic value setting in React
- Response includes multi-source search with timing and source count
- Previous selectors for mode buttons (button:has-text('快速')) are STALE — buttons don't have text
- Updated selectors.yml to v2 with correct approach

## Updated
- selectors.yml: v1 → v2 (fixed mode button selectors, added empty_state section)
- patterns.yml: v1 → v2 (updated with correct interaction patterns, added ask_suggestion_click)
